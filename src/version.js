// Versión de la app (SemVer: MAJOR.MINOR.PATCH).
// Convención: PATCH = arreglos, MINOR = funcionalidad, MAJOR = cambios
// incompatibles. Se incrementa en cada push.
export const VERSION = "0.4.0";
